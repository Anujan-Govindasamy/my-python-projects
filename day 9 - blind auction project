# TODO-1: Ask the user for input
import art
print(art.logo)
auction_data={}
length=len(auction_data)

def highest_bidder(data):
    highest_bid=0
    winner=""
    for each_bidder in data:
        bid_amount=data[each_bidder]
        if bid_amount > highest_bid:
            highest_bid=bid_amount
            winner=each_bidder
    print(f"The winner is {winner} with a closing bid of $ {highest_bid}")

bid_over=False
while bid_over==False:
    print("Welcome to the secret auction program")
    name=(input("what is your name ? "))
    price=int((input("whats your bid $ ")))
    end_bid=(input("Are there any other bidders ? type yes to continue and no to stop \n"))

    auction_data[name]=price
    if end_bid=="no":
        bid_over=True
        highest_bidder(auction_data)
    elif end_bid=="yes":
        print("\n"*50)








